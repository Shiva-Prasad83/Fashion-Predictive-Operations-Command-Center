# Production — Vercel backend URL (no trailing slash)
# This MUST be your backend Vercel URL, NOT the frontend URL
# Find it in Vercel Dashboard → your backend project → Domains
VITE_API_URL=https://REPLACE_WITH_YOUR_BACKEND_VERCEL_URL.vercel.app
